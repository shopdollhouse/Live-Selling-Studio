{
  "name": "live-selling-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host localhost --port 8080",
    "build": "vite build",
    "preview": "vite preview --host localhost --port 8080"
  },
  "dependencies": {
    "@snap/camera-kit": "^1.14.0",
    "vite": "^5.4.21"
  },
  "devDependencies": {}
}
